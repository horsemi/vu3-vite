/**
 * 权限模式
 */
export enum PermissionModeEnum {
  // role
  ROLE = 'ROLE',
  // permission
  PERMISSION = 'PERMISSION',
  // black
  BACK = 'BACK',
}

export enum TabRouterModeEnum {
  SINGLE = 'SINGLE',
  MULTIPLE = 'MULTIPLE',
}
